[project]
name = "agentskills"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "openpyxl>=3.1",
    "pytest>=8",
    "ruff>=0.8",
]

[tool.uv.workspace]
members = ["skills/uips-config-tree"]

[tool.pytest.ini_options]
testpaths = ["skills"]
